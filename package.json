{
  "name": "dsa-handbook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Problem-first DSA interview preparation: pattern ladders, worked solutions, and a schedule that survives a full-time job.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "note": "node scripts/new-note.mjs",
    "publish": "node scripts/publish.mjs",
    "transcript": "python scripts/get-transcript.py"
  },
  "devDependencies": {
    "marked": "^14.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
